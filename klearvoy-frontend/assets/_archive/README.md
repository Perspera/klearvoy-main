# Archive Notes

This folder stores legacy files that were previously in the project root.

- `root-legacy/`: historical media and document files retained for traceability.

Do not reference archived files in production pages unless they are moved back into active asset folders.
